
@model List<FileNazire>

@{
	ViewData["Title"] = "AdFileNazireList";
	Layout = "~/Views/Shared/_AdminLayout.cshtml";
	int sayac = 1;
}

<div class="col-lg-12">
	<a href="/FileNazire/AdFileNazireAdd/" class="btn btn-success mb-3">Yeni Dosya Ekle</a>

	<div class="card">
		<div class="card-body">
			<div class="card-title">
				<h4>Dosyalar</h4>
			</div>
			<div class="table-responsive">
				<table class="table">
					<thead>
						<tr>
							<th>#</th>
							<th>Dosya Tipi</th>
							<th>Dosya Durumu</th>
							<th>Güncelle</th>
							<th>İşlem</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in Model)
						{
							<tr>
								<th>@sayac</th>
								<td>@item.FileType.FileTypeTitle</td>
								<td>
									@if (item.Status)
									{
										<span class="text-success">Aktif</span>
									}
									else
									{
										<span class="text-danger">Pasif</span>
									}
								</td>
								<td>
									<a href="/FileNazire/AdFileNazireUpdate/@item.FileNazireId" class="btn btn-warning">Güncelle</a>
								</td>
								<td>
									@if (item.Status)
									{
										<a href="/FileNazire/AdFileNazireDoFalse/@item.FileNazireId" class="btn btn-danger">Pasif Yap</a>
									}
									else
									{
										<a href="/FileNazire/AdFileNazireDoTrue/@item.FileNazireId" class="btn btn-success">Aktif Yap</a>
									}
								</td>
							</tr>
							sayac++;
						}
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>

